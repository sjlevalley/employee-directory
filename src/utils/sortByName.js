



// function sortByName() {
//     // sort array ascending or descending by first name
//     if (!sorted) {
//         setEmployees(data.sort((a, b) => (a.name > b.name) ? 1 : -1));
//         setSorted(true);
//     } else {
//         setEmployees(data.sort((a, b) => (a.name > b.name) ? -1 : 1));
//         setSorted(false);
//     }
// }